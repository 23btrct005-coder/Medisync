# ── Database (uses Supabase PostgreSQL in production, H2 locally) ──
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://db.ctrnxgdtjtllzdfoynwo.supabase.co:6543/postgres?ssl=true&sslmode=require&sslfactory=org.postgresql.ssl.NonValidatingFactory}
spring.datasource.driverClassName=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:MediSync@0919}
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-init-sql=SELECT 1

# ── JPA / Hibernate ──
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# ── H2 Console (local dev only) ──
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ── JWT ──
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=86400000

# ── Server ──
server.port=${PORT:8080}
server.forward-headers-strategy=native

# ── File Upload ──
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ── Groq AI ──
groq.api.key=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

# ── Brevo Email (HTTP API) ──
brevo.api.key=${BREVO_API_KEY:}
brevo.sender.email=${BREVO_SENDER_EMAIL:}

# ── LOGGING ──
logging.level.org.hibernate.orm.jdbc.bind=TRACE
logging.level.com.zaxxer.hikari=DEBUG
logging.level.org.springframework.orm.jpa=DEBUG
