# ── Database ──
spring.datasource.url=jdbc:postgresql://db.bwjmzottkkxrdztqqeju.supabase.co:6543/postgres?prepareThreshold=0&sslmode=require
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=15
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.initialization-fail-timeout=-1
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.keepalive-time=60000

# ── JPA / Hibernate ──
spring.jpa.hibernate.ddl-auto=update
spring.jpa.generate-ddl=true
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.default_schema=public
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# ── H2 Console (local dev only) ──
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ── JWT ──
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=86400000

# ── Server ──
server.port=${PORT:8080}
server.forward-headers-strategy=native

# ── File Upload ──
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ── Groq AI ──
groq.api.key=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

# ── Brevo Email (HTTP API) ──
brevo.api.key=${BREVO_API_KEY:}
brevo.sender.email=${BREVO_SENDER_EMAIL:}

# ── MONAI AI Engine ──
monai.service.url=${MONAI_SERVICE_URL:http://localhost:8000}

# ── OpenAI AI ──
openai.api.key=${OPENAI_API_KEY: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}

# ── Razorpay ──
razorpay.key.id=${RAZORPAY_KEY_ID:}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:}

# ── LOGGING ──
logging.level.org.hibernate.orm.jdbc.bind=TRACE
logging.level.com.zaxxer.hikari=DEBUG
logging.level.org.springframework.orm.jpa=DEBUG

# ── Compression ──
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html,text/xml,text/plain,application/javascript,text/css
server.compression.min-response-size=1024

# ── Twilio (For Automated Voice & SMS) ──
twilio.account.sid=AC40ca573aef0dc8dd64db215b0531a6b3
twilio.auth.token=165fe2860fbddef1e9c720d36a76b796
twilio.phone.number=+19893682176

# ── Clinical Dispatch Preferences ──
sms.sender.id=MediSync
